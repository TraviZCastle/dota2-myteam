# Dota 2 MyTeam：五位传奇，一位名帅

设计稿 v0.1 · 2026-09-21

本稿保留最初基于 MyTeam 的设计思路。2026-09-21 用户确认正式开发范围：TI1—TI2026 历届八强、五名选手后必选第六席教练、教练参与 BP 与战术适配、不使用选手头像。以下早期规划与最新范围冲突时，以本节及 README 为准。

当前已实现 15 届 120 池、600 张选手卡及有记录的历史教练、六席选秀、最多十届生涯、选手或教练换人、无选手人物头像的图鉴，Top 3 常用英雄保留英雄头像。TI1 名单可用但逐局数据未收录；其余 560 张选手卡有 OpenDota 样本。教练缺乏记录时不虚构人物。人物在选手和教练身份间统一去重。

教练选秀规则更新：五名选手选齐后只抽年份，直接展示当届四强战队的全部已记录教练，并允许独立重抽年份一次；不再抽取战队。缺失记录的四强战队保留提示，只抽仍有合法教练候选的年份。休赛期选择教练席后采用相同规则，该次换教练独立获得一次年份重抽；取消、刷新、切换回选手候选均不重置。选手轮的两类重抽各三次保持不变，原已选教练和历史赛果不改写。

模拟参数仍为游戏规则，尚未做真实比赛校准；多位置任意换位、手动 BP、补丁复刻、同分 BO1 和 Roshan 模拟属于后续规划，不能视为已实现。

## 1. 产品定位

一个单人、离线可玩的历史职业战队组建游戏。随机遇见不同 TI 届次的选手，凑齐 1—5 号位，用自己的跨时代阵容挑战当届战队。

核心乐趣是：这一抽拿谁、哪个位置留到最后、英雄池能否支撑战术，以及下届该换谁。首版以轻量决策和直接结算为主，组队体验目标为 3—5 分钟；这只是体验目标，需试玩验证。

首页文案：**五位传奇，一路捧盾。**

## 2. 与原 MyTeam 的对应关系

| 原版 | Dota 2 版建议 |
| --- | --- |
| 年代＋球队 | TI 届次＋当届战队 |
| 球员的代表赛季卡 | 选手的赛事版本卡 |
| PG / SG / SF / PF / C | 1 Carry / 2 Midlaner / 3 Offlaner / 4 Soft Support / 5 Hard Support |
| 每池最多 15 人 | 当届实际出场选手，通常约 5 人，有替补则按证据保留 |
| 七项篮球场均统计 | K / D / A / GPM / XPM / 参战率 / 英雄伤害每分钟 |
| 四种篮球战术 | 均衡运营 / 四保一 / 抱团推进 / 抓人提速 |
| 每季 82 场 | 当届战队参加的自定义邀请赛 |
| 最多十年生涯 | 按 TI 届次推进，最多十届 |
| 每季可换一人、保存巅峰 | 每届可换一人，选手保留抽到的赛事版本 |

继承：五人去重、合法入席、两类重抽各 3 次、直接结算、本机存档、离线数据、卡面不展示总评/星级/稀有度。

## 3. 卡片和抽签

### 卡片身份

一张卡表示 `选手 × 赛事 × 战队 × 经核验的位置范围`。例如 “JerAx · OG · TI8 / 2018”。不会把一个人的整段职业生涯拼成全能版本。

同一人跨年份、跨队可以有多张卡，但同一支 MyTeam 最多拥有其一个版本。选手改名通过稳定的内部 `person_id` 关联账号及别名，不只按显示名去重。历史位置以该赛事为准，不能自动把其他年份的位置赋给当前卡。

卡片默认竖排：赛事及队名 → ID 与位置 → 七项统计 → 本届常用英雄及样本量 → 选入操作。主列表按位置 1—5 排，再按选手 ID 稳定排序，不把辅助按 GPM 排到末尾。展开后显示英雄出场次数、胜率、来源比赛与统计覆盖度。

统计口径：K/D/A 分别为每局平均；GPM/XPM 对有效比赛按时长加权；参战率为有效比赛的 `(击杀＋助攻)合计 / 团队击杀合计`，零分母为未记录；英雄伤害每分钟为有效伤害总数除以对应比赛总分钟。每项保留自己的有效样本数，不用一项缺失删掉整场其他有效数据。胜率按有效胜场/有效局数。展示一位小数，百分比及每分钟数单独标单位。

### 五名选手与一名教练

1. 等概率抽取一个仍有合法候选的 TI 届次，再等概率抽取该届一个合法战队池。
2. 每轮展示该队当届全部真实出场选手。已选人物、席位已满者仍可查看，但禁用选入并给出原因。
3. 玩家自由决定先补哪一号位；不强制按 1→5 顺序。
4. 重抽届次保留战队身份；重抽战队保留届次。每类整段生涯各 3 次，没有替代项时禁用且不扣次。仅参加一届的战队出现届次重抽不可用，是合理状态。
5. 自然进入下一轮免费抽取新池。抽签结果和扣次先落盘，再播放短动画；刷新、跳过、重复进入不获得新结果。
6. 选入前检查剩余已核验卡库是否还能用不同人物填满剩余位置，避免最后一席无解。多位置移动及互换必须双方合法。

初始卡库若不能保证五位置可完成，不能开放开始游戏。数据加载失败与真实无候选分开提示。

### 战队身份

卡面保留当届实际队名。用于跨届重抽的 `organization_key` 只在沿革证据明确时归并。赞助冠名、俱乐部品牌、选手转会、阵容整体转移分别记录，不能照搬 NBA 球队合并表，也不能仅因为五人转投新俱乐部就认作同一队。

## 4. Dota 特有的决策

### 位置与英雄池分开

1—5 号位决定资源职责，不等同于分路，也不等同于英雄属性。已核验的赛事位置由人工名单证据与比赛数据共同确定；不能单靠某场走中路就判定是 2 号位。

选手持有完整的已观察英雄记录。卡面只展示常用前三个，模拟使用完整可用池；未观察英雄不伪装成“不会玩”，对其用保守的位置先验，标为游戏估计。熟练度、英雄配合和克制参数属于内部模型，不能混进真实统计栏。

### 四种战术

| 战术 | 玩家希望发生什么 | 必须付出的代价 |
| --- | --- | --- |
| 均衡运营 | 多条获胜路径，平衡资源与地图目标 | 不特别强化某一时间段 |
| 四保一 | 优先保障 1 号位资源和存活 | 中前期主动性及其他核心资源减少 |
| 抱团推进 | 围绕强势期、塔和 Roshan 建立优势 | 阵容需要推进和持续作战能力，拖后更难 |
| 抓人提速 | 通过游走和控制创造人数差 | 依赖先手，抓人失败会损失发育 |

战术影响资源分配、英雄选择及行动倾向，不使用“选该战术固定加胜率”。阵容缺乏控制、推进或保护时，赛前给出可解释的文字提示。首版不凭名气发冠军加成，也不叠加未经验证的同队羁绊分。

### 首版自动 BP

玩家设置赛季战术，可指定选手英雄偏好；系统每小局执行简化禁选。所有对手使用相同规则、同等级的信息，不读取玩家未来随机结果。

建议自定义规则为双方各禁 2 个，再按 A-B-B-A-A-B-B-A-A-B 选满十英雄。先选方逐小局交替，系列赛首局由种子决定；阵营也按种子及交替策略分配。明确称“简化 BP”，不冒称任何版本的队长模式。

禁选前检查剩余池是否可覆盖十个席位，候选与分配采用回溯求解。英雄不重复、禁用英雄不可选，偏好与已选冲突时自动寻找合法次选，并在详情解释。小局间清空禁选，没有全局 BP 限制。后续增加可选的手动 BP 挑战模式。

## 5. 生涯与赛事

五人完成后选择一个已发布数据包中的起始 TI，按**届次**前进。最多十届，卡片保留初选赛事的状态；对手使用推进到的那一届阵容。数据包标注每个起点可玩多少届，遇到数据边界结束，不编造未来赛事、不暗中循环年份、不跳过缺失届次。

当前范围为 TI1—TI15（2011—2019、2021—2026），每届最终八强。任意已收录届次起步，最多十届，到 TI2026 截止。

### 自定义赛制

MyTeam 作为新增队伍，与该届全部已核验参赛队进行单循环 BO2。其余队也进行同样的赛程，用于形成完整排名。小局每胜计 1 分；积分相同先比较同分队相互小局积分，涉及前八晋级且仍同分时补 BO1 决胜。

前八进入固定八队双败淘汰赛：首轮 1v8、4v5、2v7、3v6；系列赛 BO3，总决赛 BO5，无重置决赛。真实当届参赛队数量可以变化，赛程据此生成。这是统一的游戏赛制，不复刻任何一届 TI 的分组和赛制。

所有比赛一次计算完成，直接显示名次、系列赛战绩、小局胜负、淘汰赛路径和五人表现。小局战绩与系列赛战绩分开标注。结果详情可展开每场 BP、关键事件及阵容问题；首版不做回放动画。

历史对手保留当届阵容，不因为 MyTeam 抽到某人就移除或补换对手队员。跨队允许与同一人物的历史版本交锋；人物去重限制作用于玩家自己的六席阵容。英雄禁选仍在每一小局的双方之间统一去重。

### 每届换一人

沿用原版当前代码的交互：先抽换人池，再决定替换哪个兼容位置。原队员直到确认新人时才离队；取消再返回保留原池。每届最多确认一人，下届生效。换人的首次抽取免费，重抽继续消耗整段生涯的剩余额度。

每届保存阵容、战术、种子、卡库/规则/模型版本和结果快照。后来换人、调战术或更新卡库都不能改写过去的成绩。浏览器无法存档时明确提示。

## 6. 模拟设计

使用独立的 Dota 模型，不能把 NBA 回合引擎换成英雄名字。

输入为五位选手的同位置标准化表现、收缩后的英雄熟练度、十英雄阵容、战术和对手。不同年代卡片映射到一套版本化的“传奇规则”，历史比赛版本用于标准化；不直接让不同时期的原始 GPM 决定胜负。每局十个英雄均受相同规则约束，不逐季复刻全部补丁机制。

内部维度建议包含：对线表现、资源转化、团战参与、推进与目标、视野与支援、英雄适配。辅助在同届同位置中比较；缺少视野等数据时只使用有证据的特征并收缩到中性先验，不能把缺失当零，也不能凭借 KDA 推导“指挥能力”。

阶段状态包括对线 → 资源争夺 → 目标推进 → 高地终结。事件共同更新经济、击杀、建筑、Roshan 和胜负；能否结束比赛取决于基地目标。禁止先抽胜负，再凭空生成一套看似能解释结果的数据。无法可靠模拟的字段暂不展示。保留随机起伏，并避免战术产生无成本、必胜的加成。

先使用明确标注的实验参数打通单届，再用按时间分割的历史比赛做校准。评估胜率校准、BO3/BO5 分布、位置偏差与战术选择多样性；完整比赛或完整赛事隔离训练与验证，避免同一系列赛泄漏。未经校准不展示夺冠概率或“真实实力排名”。

## 7. 页面与视觉

延续原版独立首页、紧凑竖排候选、右侧六席阵容、直接结果的布局。视觉改为石灰/炭黑底、朱红主操作、少量盾金用于冠军结果；随系统浅深主题调整。人物 ID 和赛事年份作为主识别，不堆满大幅人物插画或数值雷达图。

- 首页：一句主张、START、继续生涯、选手图鉴。
- 选秀：顶部两列抽签，中部竖排列出候选，右侧按 1—5 号位展示阵容。不可选原因就近显示。移动端阵容概览在前，候选列表在后。
- 备战：五人及英雄偏好、四个战术选项、起始赛事、阵容提示、查看本届结果。
- 结果：本届名次与战绩、淘汰赛路径、可展开比赛、换一人、下一届；生涯结束展示历届成绩。
- 图鉴：赛事、战队、位置过滤；具体数据来源、样本数与未记录状态。

交互草图展示选秀、备战和结果三种状态。历史数字未采集的地方用“—”；示意战术配置和演示赛果清楚标注。草图用于讨论界面，不能作为实际模拟输出。

## 8. 数据与实现

建议按比赛 ID 离线采集。基础比赛与选手字段评估 Valve Web API；聚合及解析字段优先评估 OpenDota。OpenDota 是第三方开源数据平台，其项目说明数据来自 Valve WebAPI 及比赛录像解析，不能笼统标为“Valve 官方评级”。历史名单与位置需要逐届交叉核验。

保存 `source_url`、抓取时间、内容哈希、`match_id`、`league_id`、`team_id`、人员及角色证据、比赛补丁和覆盖状态。先用真实比赛确认候选，不凭全职业生涯汇总猜测某届单队统计。接口可用不等于早年解析字段齐全；第一步审计 TI8 的全部名单和逐场覆盖。

公开卡库建议字段：

```text
card_id, person_id, event_id, event_label,
team_id, historical_team_name, organization_key,
positions[], position_evidence[], matches_played,
stats{value, valid_matches, scope}[],
heroes{hero_id, picks, wins}[], source_match_ids[], verification_status
```

模拟资料单独保存，包含 profile、sample_confidence、hero_model 和规则版本。隐藏能力仅表示界面不展示，离线前端资源仍可被技术用户读取。

建议沿用原项目的静态 HTML/CSS/JavaScript＋Python 构建脚本，不为单人离线玩法增加后端。独立目录 `Dota2MyTeam/`，使用独立存档命名空间 `dota2myteam.run.v1`。

复用 `game.js` 的种子随机、抽签状态、合法席位、互换、换人与快照思想，抽出通用规则；不直接复制 NBA 的赛季边界和 82 场校验。重写 `simulation.js`、赛事赛程、名单身份映射、角色判断及数据采集。新模块建议为 `draft.js`、`roles.js`、`auto-draft.js`、`tournament.js`、`simulation.js` 与 `storage.js`。

### 实施顺序和验收

1. **数据小样**：审计 TI8 全部参赛队与比赛覆盖；有五位置的完整可选卡库，原始统计能回溯，缺失字段不补造。
2. **单届闭环**：五轮选秀、两类重抽、四战术、自动 BP、自定义赛事、结果、本机存档。确认不能抽入死局，辅助与核心都能形成有效策略。
3. **历史生涯**：逐届扩展已核验数据，加入每届换一人、连续赛事边界及最多十届记录。
4. **进阶玩法**：手动 BP、更多赛事和更细的版本模型；在数据与模型支持后再做。

关键检查：别名去重、历史位置、抽签不返还次数、换人不能改历史、双败淘汰及决赛终止、英雄禁选合法性、同种子复现、得失事件一致性、无未采集统计混入真实栏。UI 检查覆盖桌面/窄屏、键盘操作、长 ID 和缺失值。

首版范围不包括账号、交易市场、付费抽卡、多人联机、排行榜、选手养成、实时操作英雄与完整录像回放。

## 9. 已查阅的外部依据

- [OpenDota 项目说明](https://github.com/odota/core)：公开说明 Valve WebAPI 与 replay parsing 的数据来源。
- [OpenDota API 文档](https://docs.opendota.com/)：后续逐项核查采集接口及字段覆盖。
- [Valve：The International Grand Champions，2018-08-27](https://store.steampowered.com/news/posts/?appids=570&enddate=1535469073)：确认 TI8 OG 的五名选手及赛事背景。本文未据此编造选手统计。

尚未完成：完整赛事卡库采集、所有队伍及历史位置核验、模型校准、可运行的正式游戏。
